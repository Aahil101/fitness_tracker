project_id = "hoahlhknjjfoketgqkul"

[auth]
# Canonical production alias; the second alias is the project's auto-generated
# production domain and stays allowed so either URL works.
site_url = "https://frontend-fitness-575c.vercel.app"
additional_redirect_urls = [
  "https://frontend-fitness-575c.vercel.app",
  "https://frontend-fitness-575c.vercel.app/",
  "https://frontend-nine-mu-goi9olqbjw.vercel.app",
  "https://frontend-nine-mu-goi9olqbjw.vercel.app/",
]

[auth.email]
# Confirmation is disabled in the dashboard; mirrored here so a config push
# does not silently re-enable it.
enable_confirmations = false
# Pinned to the values already live on the remote project so pushing the
# redirect URLs does not drift unrelated OTP settings.
max_frequency = "1m0s"
otp_length = 8
